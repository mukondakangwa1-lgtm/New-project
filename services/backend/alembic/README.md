"""
Alembic script placeholder. Developers should run `alembic revision --autogenerate -m "initial"`
from the services/backend directory to generate a versions file. This repository includes the
alembic scaffold but does not auto-generate migrations in CI.
"""
